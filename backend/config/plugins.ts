// src/config/plugins.ts

export default {
    // Navigation plugin configuration
    'navigation': {
      enabled: true,
      config: {
        navContentTypes: ['main-navigation'], // Specify the navigation slugs to be used
      },
    },
  
     
  };
  